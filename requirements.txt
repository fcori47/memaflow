# Core (todas las plataformas)
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.24.0
pynput>=1.7.6
pyperclip>=1.8.2

# Windows / Linux: icono en la bandeja del sistema
pystray>=0.19.5 ; sys_platform != "darwin"
pillow>=10.0.0 ; sys_platform != "darwin"

# macOS (Apple Silicon): transcripcion en GPU/Metal + overlay nativo (NSPanel)
mlx-whisper>=0.4.0 ; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0 ; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0 ; sys_platform == "darwin"
pillow>=10.0.0 ; sys_platform == "darwin"
